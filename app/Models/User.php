<?php

// php artisan make:model User
// merepresentasikan tabel users dalam basis data

namespace App\Models;

// use Illuminate\Contracts\Auth\MustVerifyEmail;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;

//User merupakan sebuah model yang mewarisi dari class Authenticatable. Class ini digunakan untuk autentikasi pengguna, seperti login dan registrasi.
class User extends Authenticatable
{
    use HasFactory, Notifiable;

    /**
     * The attributes that are mass assignable.
     *
     * @var array<int, string>
     */
    //mendefinisikan atribut atau kolom pada tabel users yang dapat mass assignable (memungkinkan data untuk diisi secara massal ketika kita membuat atau memperbarui data pengguna.)
    protected $fillable = [
        'name',
        'email',
        'password',
        'role',
    ];

    /**
     * The attributes that should be hidden for serialization.
     *
     * @var array<int, string>
     */
    protected $hidden = [
        'password',
        'remember_token',
    ];

    /**
     * Get the attributes that should be cast.
     *
     * @return array<string, string>
     */
    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }
}
